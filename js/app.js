// Boot placeholder — replaced by the real bootstrap once the store and
// views land. Keeping index.html's module graph valid from the first commit.

const view = document.getElementById('view');
if (view) {
  view.innerHTML = '<div class="boot-message"><p>Seedling planted — app coming up in the next commits.</p></div>';
}
